# Order-Fix — 2026-06-16

STATUS: ANGEHALTEN (blockiert) — irreversible/outward-facing Schritte NICHT ausgefuehrt.

Auftrag (temp-Datei) verlangt "AUTONOM, keine Rueckfragen" und nimmt einen Order-Bug
seit 01.06. an. Diese Praemisse wird durch die LIVE-Faktenlage (Code + state.json,
heute geprueft) NICHT bestaetigt. Die beiden unumkehrbaren Schritte (Live-Orders ins
Google-Sheet schreiben + Auto-Order-Block reaktivieren) wurden bewusst angehalten und
warten auf explizite Nutzer-Freigabe.

---

## Verifizierte Fakten (heute, gegen Live-Code + state.json)

### Fix (a) — "appendOrder seit 01.06. tot": KEIN Bug
- Order-Fill-Block `cockpit-morning.py:1342-1354` ist auskommentiert — bewusst am
  2026-05-18 (commit a6aa848) zur Phantom-Order-Vermeidung, dok. in
  reports/fillrate-rootcause-2026-05-15.md. Nicht "mitgetoetet".
- Schreibpfad `journal.js writeMultiple` (Append `Orders!A:W`) ist intakt.
- Echter Grund fuer 0 Orders: Pipeline entscheidet seit 2026-06-02 taeglich WAIT.
- Live state.json (_last_run 2026-06-16T08:17): captain.entscheidung =
  "WAIT: Konfidenz 0.367 LONG < 0.40-Schwelle ... FOMC MORGEN 20:00 -> kein Chase",
  "0 Orders".

### SCHRITT 5 — die 3 Orders widersprechen der System-Analyse
- state.json flaggt DJ30 + SPX500 heute als long-ok.
- Auftrag verlangt US30 Limit SELL und SP500 Limit SELL = Gegenrichtung.
- Tag vor FOMC (System: "kein Chase"). Live-Schreiben dieser Orders = genau die
  Phantom-Orders, die der 05-18-Fix entfernt hat, und nicht rueckholbar.
- DAHER NICHT geschrieben. Freigabe noetig.

### Fix (b) — Duplikat-Docs: nicht aus diesem Repo fixbar
- Node-Pfad (createReport/appendKiAnalyse) ist bereits idempotent (Titel-/Marker-Check).
- Dup-Quelle ist ein an das Google-Sheet gebundenes Apps Script (Trigger ohne eigene
  Idempotenz, feuert bei jedem ReportData!A1-Write). Liegt NICHT im Repo, hier nicht
  aenderbar. Node-seitige Aenderung wuerde die Ursache nicht treffen.

### Fix (c) — nan-Preis US30/SPX500: vorgeschlagener Fix ist bereits drin
- TV_SYMBOL_MAP:277-281 mappt ^GSPC->Vantage:SP500, ^DJI->Vantage:DJ30 bereits korrekt
  (GER40 nutzt dasselbe Schema und funktioniert). Symbol-Mapping ist NICHT die Ursache.
- Ursache: intermittierender Doppel-Ausfall (CDP-Symbol nicht rechtzeitig geladen +
  yfinance ^GSPC/^DJI intraday leer). current_price=None -> generate_order returnt None.
- Additiver, reversibler Fix moeglich (yfinance-Retry / robusterer Fallback). NICHT
  angewandt, weil ein stale-Preis aus state.json fuer echte Entry/SL/TP-Berechnung
  fragwuerdig ist und Order-Math beeinflusst. Vorschlag wartet auf Freigabe.

---

## Backups
KEINE — es wurden (bewusst) keine Dateien geaendert. Backups folgen unmittelbar vor der
ersten echten Aenderung, sobald freigegeben.

## Dry-Run
NICHT ausgefuehrt. Keine Orders ins Live-Sheet geschrieben.

## Offen / Nutzer-Entscheidung noetig
1. (a/SCHRITT 5) Sollen Orders ENTGEGEN der heutigen WAIT-Entscheidung + entgegen der
   long-ok-Richtung von DJ30/SPX500 ins Live-Journal geschrieben werden? (irreversibel)
2. (a) Auto-Order-Block 1342-1354 dauerhaft reaktivieren? (reaktiviert Phantom-Order-
   Problematik von 05-18)
3. (b) Apps-Script-Editor-Zugriff am Google-Sheet noetig — ausserhalb dieses Repos.
4. (c) Freigabe fuer additiven yfinance-Retry/CDP-Robustheits-Fix (Symbol-Map bleibt).
